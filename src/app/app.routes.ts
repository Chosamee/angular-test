import { Routes } from '@angular/router';
import { SseTestComponent } from './components/sse-test/sse-test.component';

export const routes: Routes = [
  {
    path: 'test',
    component: SseTestComponent,
    title: 'asdf',
  },
];
